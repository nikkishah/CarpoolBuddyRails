<h1>Listing children</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Highschool</th>
      <th>Sex</th>
      <th>Grade</th>
      <th>Passcode</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @children.each do |child| %>
      <tr>
        <td><%= child.name %></td>
        <td><%= child.highschool %></td>
        <td><%= child.sex %></td>
        <td><%= child.grade %></td>
        <td><%= child.passcode %></td>
        <td><%= link_to 'Show', child %></td>
        <td><%= link_to 'Edit', edit_child_path(child) %></td>
        <td><%= link_to 'Destroy', child, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Child', new_child_path %>
